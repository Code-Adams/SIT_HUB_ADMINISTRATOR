<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.SakshmBhat.sit_hub_administrator">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/Namma_SIT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feed.DeleteFeedActivity"></activity>
        <activity android:name=".faculty_list.UpdateFacultyActivity" />
        <activity android:name=".faculty_list.AddFacultyActivity" />
        <activity android:name=".faculty_list.UpdateAndAddFacultyActivity" />
        <activity android:name=".UploadEbookActivity" />
        <activity android:name=".UploadGalleryImageActivity" />
        <activity android:name=".feed.UploadFeedActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest> 